### v2.0.1

**🔧 参考图分辨率自适应 + legacy 文案动态化**

* 自拍/补拍有衣橱参考图或用户图时，按后端类型自动覆盖分辨率：openaiimagesurl→4K，arkseedream→1K；其他后端不覆盖。
* 无参考图时仍使用各自默认尺寸配置；有参考图时分辨率优先于用户指定的比例。
* 补拍链路和对话 LLM 路径中硬编码的"图4"/"前三张"等参考图序号改为动态生成，随 WebUI 参考图数量配置自动适配。

---

### v2.0.0

**👀 衣橱参考图注入对话模型**

* 新增 `wardrobe_preview_to_llm` 开关，开启后 `aiimg_wardrobe_preview` 会把参考图作为图片返回给对话模型。
* 对话模型看图后自主决定是否使用该参考图及参考哪些维度（服装/姿势/场景）。
* `aiimg_generate` 新增 `use_wardrobe_ref` 参数，看图后判断不匹配可传 false 跳过该参考图。

---

### v1.9.9

**🎨 r2/r3 多模态参考图 + cosplay 风格加权**

* 补拍链路的 r2 创意设计师和 r3 审核师现在能直接看到衣橱参考图本身，而非仅看文本描述。
* 第 1 次调用带图片，失败时第 2 次自动回退为纯文本描述重试。
* 参考图文件缺失或不可用时整体回退为纯文本描述模式。
* 补拍选风格时 cosplay 权重提升至 10 倍，更频繁选中。
* cosplay 选中时强制确保有参考图，设计师不凭空合成。
* cosplay 找不到参考图时自动换一个其他风格重试。

---

### v1.9.8

**🔧 /改图 计入补拍额度**

* `/改图` 成功后，实际使用的服务商会计入所有配置了该服务商的补拍人格的当日额度。
* `/补拍状态` 现在能反映 `/改图` 的真实用量，补拍不会再因看不到手动用量而超支。

---

### v1.9.7

**🎯 Ark Seedream 后端单图模式**

* 补拍链路检测到服务商是 Ark Seedream 时，只传人设参考图，不传衣橱图。
* 混合服务商人格（Ark + 非 Ark）在生图环节兜底处理。

---

### v1.9.6

**🔧 修复 LLM 工具返回信息冲突**

* 修复 LLM 同时收到插件中文消息和框架英文指令、语义冲突的问题。
* 现在 LLM 只收到插件的单一中文消息，知道图片已发送，无需重复调用发送工具。

---

### v1.9.5

**🎯 新增 /补拍 命令（单提供商立即补拍）**

* 新增 `/补拍 @提供商ID` 命令：针对单个提供商立即补拍，只消耗该提供商的剩余额度。
* 使用场景：某提供商未在定时任务中自拍或额度未耗尽时，手动触发。
* 删除旧的 `/补画` 命令，`/补画状态` 改名为 `/补拍状态`。

---

### v1.9.4

**🖼️ 新增 Ark Seedream 专用后端**

* 新增 Seedream 5.0 pro 专用后端，修复该模型改图时 HTTP 400 错误。
* 行为与 OpenAI ImagesURL 后端一致，唯一区别是改图时不发送该模型不支持的参数。
* 旧模板保持原样不动，不影响现有用户。

---

### v1.9.3

**🔍 /补拍debug 命令 + 提供商名显示**

* 新增 `/补拍debug` 命令：输出当日补拍摘要（触发人格数、设计批次数、超时次数、延迟重试次数、最终成功/失败）并列出事件流。
* 补拍日志显示具体提供商名，不再只显示数量。

---

### v1.9.2

**🛡️ 补拍设计失败延迟重试 + 跨日保护**

* 补拍创意设计批次整体失败后不再直接跳过，改为等待 10 分钟后延迟重试，最多 2 次。
* 新增 23:30 跨日保护截止线，避免生图跨日导致额度计算错乱。

---

### v1.9.1

**✨ 审核师系统提示词人格级定制**

* 补拍审核师系统提示词现支持按人格单独配置，与创意设计和提示词构建保持一致。
* 用户可根据角色特征调整审核尺度（如某角色对发型约束更严格）。
* 配置留空时自动回退到插件内置默认提示词。

---

### v1.9.0

**🚀 补拍链路重构：算法选风格 + 服装审核环节**

* **算法选风格**：第 1 轮 LLM 选风格替换为算法实现（近期去重 + 加权随机），避免 LLM 反复挑同一个风格。
* **新增服装审核环节**：设计师输出后、提示词翻译前，插入审核师对每套设计做美学审查，未通过的设计直接给改进版。
* **重写设计师系统提示词**：吸收设计哲学——色彩/廓形/材质三要素锚定、经典搭配优先、单品必要性原则。
* **全局 provider 配置**：四个轮次的 LLM 提供商全部改为全局级配置，留空则回退到默认对话模型。
* 流程编排重构：算法选风格 + LLM 选场景并行 → 设计师 → 审核师 → 提示词翻译。

---

### v1.8.3

**🔧 补拍额度计数策略重构**

* 修复手动自拍额度计数与服务商实际消耗对不上的问题。
* 额度计数改为按实际成功的提供商计算，而非链路第一个。
* 额度计数 key 改为 `人格名::提供商ID`，支持按人格隔离。

---

### v1.8.2

**🐛 修复 Grok Video 轮询状态识别失败导致请求爆炸**

* 修复中转站轮询返回大写 `SUCCESS` 状态时无法识别，导致任务成功但代码持续重试。
* 状态识别支持大写 `SUCCESS`/`ERROR`。

---

### v1.8.1

**🐛 修复引用图片生成视频全部失败 + 后端重命名**

* 修复图生视频失败：AstrBot 在事件结束后清理临时图片文件，异步任务读图时已不存在。改为提前预提取图片字节。
* `grok_video_3` 模板重命名为 `grok_video_multipart`，保留旧名兼容。
* `aspect_ratio` 从下拉框改为自由字符串输入。

---

### v1.8.0

**🚀 视频后端重构：新增 Grok Video 3 + 多模型级联**

* 移除云雾旧格式后端（旧配置需手动改为新后端）。
* 新增 `grok_video_3` 视频后端模板：基于 multipart 协议，适用于 PoloAI / s.apifox 等兼容接口。
* 新增多模型级联：同一后端下按顺序尝试多个模型名，失败自动切换。所有视频后端新增 `models` 字段。

---

### v1.7.2

**✨ 提示词构建系统提示词人格级暴露**

* 补拍第 4 轮「提示词构建」系统提示词现支持按人格单独配置。
* 用户可根据角色特征调整提示词构建风格。
* 配置留空时自动回退到插件内置默认提示词。

---

### v1.7.1

**✨ 补拍风格池人格级定制**

* 补拍风格池现支持按人格单独定制，配合衣橱插件的人格级风格池功能。
* 人格级风格池留空时自动回退到衣橱全局风格池。

---

### v1.7.0

**✨ 创意设计系统提示词人格级暴露**

* 补拍第 3 轮「创意设计」系统提示词现支持按人格单独配置。
* 用户可根据角色特征自行调整：例如不戴眼镜的角色可删除眼镜约束，喜欢穿裙子的角色可添加裙装偏好。
* 配置留空时自动回退到插件内置默认提示词。

---

### v1.6.9

**🚀 官方 Grok 视频后端适配**

* 修复官方 Grok 视频后端的图片字段格式 Bug，符合 xAI 官方 API 规范。
* 新增多参考图数组支持。
* 新增视频编辑接口。

---

### v1.6.8

**🐛 修复 image 模式下 LLM 重复发送图片**

* 修复 LLM 调用画图工具后重复调用发送工具、导致用户收到重复图片的问题。
* 新增运行时补丁，将框架指令替换为"图片已直接发送，不要再次发送"。

---

### v1.6.7

**🔄 适配 QZone v4.0.0**

* 适配 QZone 插件 v4.0.0 Daemon 架构重构。

---

### v1.6.6

**🚀 补拍架构重构：两轮→四轮**

* 补拍提示词生成从两轮架构重构为四轮：风格选择与场景生成并行 → 创意设计 → 提示词构建。
* 风格和场景独立生成，打破刻板耦合（如"汉服→书法""JK→教室"），让画面更多元化。
* 修复有参考图但无描述时参考图丢失的问题。

---

### v1.6.5

**🐛 修复视频发送超时误判导致重复发送**

* 修复本地文件发送遇到协议端超时时，消息实际已送达但被误判为失败，导致用户收到重复视频。
* 超时类错误视为消息可能已送达，不再尝试其他发送方式。

---

### v1.6.4

**🐛 修复补拍发空间图片上传失败**

* 修复补拍发空间图片上传失败：无条件重编码为 baseline RGB JPEG，确保剥离所有不兼容的色彩模式和渐进式编码。
* 转换失败则跳过该图片。

---

### v1.6.3

**🐛 修复补拍发空间图片上传失败**

* 修复补拍发空间图片上传失败：改为主动读取图片 bytes 并确保格式兼容后传递给 QZone 插件，不再依赖 URI 链路。

---

### v1.6.2

**🚀 新功能：per-provider output + 比例参数 + 自定义 UA**

* 自拍链路支持 per-provider output 覆盖：每个服务商可单独设置输出分辨率。
* 改图命令支持末尾比例参数：`/改图 换背景 16:9`。
* 比例参数支持中文冒号：`16：9` 自动规范化。
* 文生图默认输出尺寸可留空，由服务商默认决定。
* 服务商支持自定义 User-Agent 请求头，可绕过中转站 UA 拦截。

---

### v1.6.1

**🔄 提示词优化：适配 4 字段结构**

* 豆包创意设计师输出从 3 字段升级为 4 字段（新增 appearance 外观造型字段，承载发型+指甲油）。
* 豆包系统提示词全面重写。
* DeepSeek 提示词优化大师系统提示词全面重写。

---

### v1.6.0

**🚀 新功能：无参考图创意设计分流**

* 补拍搜图无参考图时，可调用创意设计提供商进行专业服装与场景设计，再由对话模型生成提示词。
* 人格级新增 `costume_designer_provider_id` 配置：选择创意设计提供商，留空则由对话模型自行构建提示词。
* 创意设计提供商调用失败时自动重试一次，仍失败则优雅降级为现有无图自由发挥行为。
* 补拍批次大小从 5 降为 3，降低单次 LLM 压力。

---

### v1.5.2

**✨ 新增：补拍衰减过滤联动**

* 补拍搜图时启用衣橱插件的补拍衰减过滤，避免近期重复使用同一张参考图。

---

### v1.5.1

**🐛 修复：生图/视频后端隔离失效**

* 修复视频后端能被生图工具错误调用的问题。
* 服务商自动根据模板类型区分图片/视频后端，生图工具只能用图片后端，视频工具只能用视频后端。

---

### v1.5.0

**🐛 修复补拍多提供商不切换后端 + 移除人格级补拍时间**

* 修复补拍多提供商不实际切换生图后端的 bug。
* 额度是提供商级共享资源，多个人格配置同一提供商时额度共享。
* 移除人格级补拍时间配置字段：补拍时间已改为提供商级。
* 优化配置提示：明确说明每个字段的含义和额度共享规则。

---

### v1.4.9

**🐛 修复：补拍多提供商不实际切换生图后端**

* 修复多提供商额度计数正确、但实际生图始终走人格链路的 bug。
* 生图环节现在使用对应提供商后端，重试路径同步修复。

---

### v1.4.8

**🐛 修复：后台生成模式下 LLM 自拍不计入补拍额度**

* 修复后台生成模式（默认开启）时 LLM 自拍不消耗每日补拍额度的恶性 bug。
* 两条路径（前台/后台）统一调用额度追踪。

---

### v1.4.7

**🔧 补拍调度时间改为提供商级 + 按提供商分别发布空间**

* 每个服务商可独立设置补拍触发时间。
* 同一人格的不同服务商可在不同时间分别触发。
* 发空间改为每个服务商完成后分别发布，不再合并一次性发布。
* `/补拍状态` 输出增加每个服务商的调度时间显示。

---

### v1.4.6

**🐛 修复：补拍发空间配文图片临时文件名冲突**

* 修复同名图片或连续补拍时临时文件被覆盖导致 LLM 配文图片错乱。

---

### v1.4.5

**🐛 修复：补拍发空间多项稳定性问题**

* 修复 LLM 配文图片数量超限。
* 修复配文生成失败时图片完全不发，现用日期作为回退配文继续发布。
* 修复 Qzone 图片数量无上限可能被 API 拒绝，限制为 9 张。
* 修复临时压缩图片文件未清理导致堆积。
* 修复重试成功的图片未加入成功列表导致不发空间。
* 修复重试时使用错误的提示词。

---

### v1.4.4

**🐛 修复：补拍指引 + 空间配文超限/超时**

* 补拍参考图指引改用专用常量，不再引用 LLM 无法理解的内部技能名称。
* 空间配文生成前压缩图片，避免多图 payload 超 API 限制。
* 空间配文生成超时从 120s 增至 600s，首次失败后自动重试一次。

---

### v1.4.3

**🔧 修复：视频后端 WriteTimeout**

* 所有视频后端 write timeout 统一增大至 120s，解决大图 Base64 上传超时导致提供商后台收不到请求的问题。

---

### v1.4.2

**🐛 重要修复：LLM 自拍不计入补拍额度**

* 修复 LLM 自拍模式生成的自拍不计入每日补拍额度的恶性 bug。
* 修复 wardrobe_preview 返回文本双句号。
* 优化指引文字。

---

### v1.4.1

**🐛 修复：生图工具能看到视频后端名称**

* 修复 LLM 用视频后端名调用生图工具的错误。
* 生图工具只看图片后端，视频工具只看视频后端。

---

### v1.4.0

**🐛 重要修复：LLM 引用图片生成视频全部失败**

* 修复 LLM 传入本地路径作为 image_url 时视频生成失败的 bug。
* 非远程链接的 URL 自动转为 data URL。
* 优化压缩函数：压缩后比原始大时返回原始，避免无谓的体积膨胀。

---

### v1.3.9

**🚀 新功能：自拍可禁用衣橱参考图**

* aiimg_generate 新增 `use_wardrobe` 参数：LLM 可控制是否使用衣橱参考图。
* 默认 true（保持现有行为），用户说"不用衣橱"时 LLM 可设为 false。

**📝 优化：LLM 返回文本提醒**

* 图片生成返回文本加"无需调用 send_message_to_user"提醒，防止 LLM 重复调用发送工具。

---

### v1.3.8

**🚀 新功能：TrueGrok 级联模板**

* 新增 TrueGrok 组合模板：配置 fallback_chain（最多 3 个），按顺序尝试，失败自动切换。
* 典型用法：真 Grok（便宜/不稳定）→ 假 Grok（贵/稳定），省钱同时保成功。

---

### v1.3.7

**🚀 新功能：provider_id 快捷命令**

* 支持 `/<provider_id> <提示词>` 格式：直接用服务商 ID 作为命令。
* 图片类服务商 → 自动转为改图，视频类 → 自动转为视频生成。
* 原有命令不受影响，完全共存。

---

### v1.3.6

**🚀 新功能：LLM 可指定视频后端**

* aiimg_video 工具新增 `backend` 参数：LLM 调用时可指定用哪个视频服务商。
* `backend=auto`（默认）时走全局视频链路，不影响现有行为。

---

### v1.3.5

**🚀 新功能：假 Grok 视频提供商 + 精简模板**

* 新增 FakeGrokVideoService：基于 JSON 接口的 Grok 视频生成。
* 删除 9 个不用的提供商模板（Gemini 原生、Flow2API、Vertex 匿名、Grok Images 等）。

---

### v1.3.4

**🚀 新功能：真 Grok 视频提供商**

* 新增 RealGrokVideoService：基于 Yunwu API 的真 Grok 视频生成（图生视频/文生视频），异步轮询模式。
* 支持时长、宽高比、超时、轮询间隔等配置。
* 修复 input_reference 格式：Yunwu API 只接受 URL 字符串，不支持文件上传。

---

### v1.3.3

**🔧 优化：补拍搜图收紧 + 无参考图自由发挥**

* 新增 `daily_selfie_ref_min_similarity` 配置项：补拍搜参考图的相似度阈值，设为 0.6~0.7 可增加"无参考图自由发挥"的比例。
* 补拍有参考图时一律使用 reimagine 强度：仅借服装款式，姿势构图完全重新设计。
* 补拍无参考图时指引增强：要求用自然连贯长句详细描述画面。

---

### v1.3.2

**🚀 新功能：补拍完成后自动发空间说说**

* 补拍完成后自动将生成的图片发布到 QQ 空间说说，角色以第一人称写日常分享配文。
* 人格级配置：开关和多模态 LLM 提供商可独立配置，与补拍对话模型分开。

**🔧 优化：间隔拉长 + 失败自动重试**

* 补拍请求间隔从 5s 拉长到 30s，避免短时间内大量并发冲击 API。
* 新增失败自动重试开关（默认开启）：补拍失败或超时的图片会自动重新生成。
* 全局补拍默认时间从 23:30 提前到 23:00，给重试留出更多时间窗口。

---

### v1.3.0

**🚀 新功能：人格级视频生成 + 人格级补拍时间**

* 视频生成支持人格级：每个人格可独立指定视频服务商链路。
* 补拍触发时间支持人格级：每个人格可设置独立的补拍触发时间，留空则使用全局设置。
* cron 循环改为多时间段感知：到点只运行匹配当前时间的人格。

---

### v1.2.9

**🔧 优化：wardrobe_preview 提示词增强**

* 返回值中新增"前 N 张参考图是你的人设图"说明，LLM 现在能明确区分人设图与衣橱参考图。

---

### v1.2.8

**🐛 Bug 修复 + 搜图并行化**

* 修复补拍任务全局锁：A 人格补拍时 B 人格命令被拒绝，改为按人格隔离。
* 修复参考图序号错乱：补拍和手动取图的参考图序号现在与实际 images 列表索引对齐。
* 搜图改为并发搜索，不再逐条串行等待。

---

### v1.2.7

**✨ 新功能：后台生成模式**

* LLM 调用画图工具时不再阻塞对话，图片在后台生成完成后自动发送，期间可继续聊天（配置项 `llm_tool.background_generate`，默认开启）。
* 关闭后台模式后恢复原有行为：等待图片生成完毕，LLM 可在上下文中看到图片。

---

### v1.2.6

**🔧 优化：统一参考强度指引**

* 补拍与手动取图现在共用参考强度指引函数，措辞完全一致。
* 参考图编号格式与手动取图对齐。

---

### v1.2.5

**🐛 Bug 修复 + 补拍对话模型可配置**

* 露脸规则提升为最高优先级：当参考图描述包含挡脸/遮脸姿势时，LLM 必须改为面部完整朝向镜头的替代姿势。
* 搜图匹配不上时不再浪费额度：为缺失参考图的查询生成"无参考图自行发挥"描述，确保 N 个额度生成 N 张图。
* 补拍对话模型改为可配置：WebUI 新增"补拍对话模型"下拉选择器，留空则回退到默认模型。

---

### v1.2.4

**🐛 Bug 修复 + 补画并发化**

* 修复补画流程变量名拼写错误导致中断。
* 修复补画第二轮 LLM 返回空文本时静默失败，增加降级重试。
* 修复补画时改图默认输出尺寸覆盖后端默认尺寸的问题。
* 补画画图从串行改为并发：每隔 5 秒发一个请求，不等前一张画完。
* 取消补画发送图片给用户的功能，补画结果仅静默存入衣橱。
* LLM 调用增加超时保护（120s）、画图增加超时保护（300s）。
* `/补画` 命令支持只处理当前人格。
